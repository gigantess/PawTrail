# [프로젝트 기획서] 반려견 맞춤형 안심 산책 에이전트 및 기록 플랫폼 (PawTrail)

## 1. 프로젝트 개요
* **프로젝트명**: PawTrail (포트레일)
* **부제**: 반려견의 관절 안심·무계단·완만한 경사·그늘 우선 AI Native 산책 플래너 및 기록 플랫폼
* **기획 배경 및 방향성**:
  - 도심지 반려견 산책에서 가장 빈번하고 심각하게 발생하는 문제는 **계단으로 인한 관절 부담, 급경사로 인한 피로, 직사광선/한낮 열 위험, 현장의 돌발 장애물(높은 턱, 공사 등)**입니다.
  - 기존 일반 지도 서비스는 목적지 최단거리 위주의 보행 경로만을 제공하여 계단 유무, 경사 수준, 시간대별 그늘 여부를 파악할 수 없습니다.
  - PawTrail은 **반려견의 상태와 산책 조건을 이해하는 AI Agent가 지도·라우팅·환경 데이터를 활용하여 확인된 계단과 과도한 경사를 회피하고, 가능한 범위에서 그늘을 우선하는 개인화 산책 코스를 생성**합니다.
  - **프라이버시 중심 로컬 저장 (Privacy-First & Local-First)**:
    - 견주의 자택 위치, 상세 보행 GPS 궤적, 반려견 프로필 등 민감한 개인정보는 **사용자의 휴대폰(로컬 스토리지/IndexedDB)에만 안전하게 보관**하며 서버에 축적하지 않습니다.
    - 외부 소셜 로그인의 복잡성과 의존성을 배제하고, 커뮤니티 코스 공유 및 위험 제보 관리를 위해 **최소한의 간단한 이메일 회원가입 체계**만을 운영합니다.
  - 산책 중에는 **현장 위험 요소(높은 턱, 계단, 공사 등)를 Vision AI로 분석해 우회를 권장**하고, 커뮤니티 집단지성을 통해 안전한 산책 정보를 공유합니다.
  - **신뢰도 및 한계 명시 원칙**: OSM 지도 데이터에 등록된 계단 구간을 성실히 탐색·회피하되 "100% 무계단 보장"과 같은 과장된 표현을 사용하지 않으며, 데이터 출처와 신뢰도를 투명하게 안내합니다.
* **핵심 타깃**:
  - 슬개골 등 관절 관리가 필요하여 계단이나 가파른 언덕을 피해야 하는 소형견·노령견 견주
  - 여름철이나 낮 시간대 직사광선과 과도한 열 노출을 피해 그늘길 위주로 산책하고 싶은 견주
  - 자택 위치 등 개인정보 유출 걱정 없이 안전하게 맞춤 산책 코스를 추천받고 기록하고 싶은 견주
* **실사용자 테스트 목표**: 반려견 견주 최소 5인 이상 대상 실제 필드 테스트 및 피드백 반영

---

## 2. 문제 정의 및 AI 기반 해결 방식

| 문제점 (Pain Points) | AI 및 데이터 기반 해결 방식 | 적용 기술 및 데이터 출처 |
|---|---|---|
| **계단 통행으로 인한 관절 부담**<br>(소형견·노령견 관절 질환 취약) | OSM 보행망의 `highway=steps` 속성을 사전에 필터링하여 **지도 데이터상 확인된 계단 구간을 최우선 회피**하는 순환 경로 생성 (대체 경로 부재 시 제한사항 명시) | **OSM Steps Detector & Routing Adapter** |
| **과도한 경사로 인한 보행 피로**<br>(가파른 오르막·내리막 부상 위험) | DEM 고도 데이터 및 라우팅 고도 프로파일을 연계하여 **최대 경사도(`max_slope_percent`) 및 급경사 구간 비율을 제어**, 완만한 평지 위주 경로 도출 | **Elevation/DEM Slope Analyzer** |
| **직사광선 노출 및 열 스트레스**<br>(여름철 복사열 및 탈진 위험) | 산책 일시와 위치 기반 태양 고도/방위각(SunCalc)과 확보 가능한 건물 형상/높이 데이터를 결합하여 **예상 그늘 구간을 우선 배정** | **Solar Model & Shade Estimator (Level 1~2)** |
| **현장 돌발 장애물 및 시각적 위험**<br>(높은 턱, 공사 구간, 깨진 유리) | 산책 전/중 현장 사진을 **Vision AI가 분석하여 높은 턱·계단·보행 방해 요소를 감지**하고 안전 우회 경로 권장 (※ 사진만으로 지면 온도를 측정한다고 단정하지 않음) | **Multimodal Vision Inspector** |
| **자택 위치 및 반려견 정보 유출 우려**<br>(프라이버시 침해 우려) | 민감한 자택 위치, 산책 궤적, 반려견 신체 정보를 서버 DB에 모으지 않고 **사용자 휴대폰에만 로컬 보관**. AI 요청 시 일회성 페이로드로 전송하는 무상태(Stateless) 처리 | **Local-First Architecture (IndexedDB)** |
| **자연어 기반 복합 산책로 생성 요구** | "10살 시츄, 관절 무리 없게 계단 피하고 그늘 많은 20분 코스 추천해줘" 요청을 분석하여 조건 구조화, 도구 호출, 후보 경로 평가를 자율 오케스트레이션 | **LangGraph ReAct Agent** |

---

## 3. 시스템 아키텍처 및 핵심 기술 명세

### 3.1 시스템 개념 아키텍처 다이어그램
```text
  [ 사용자 입력 (자연어 발화 / 산책 조건 선택 / 현장 사진) ]
                     │
                     ▼
  ┌─────────────────────────────────────────────────────────────┐
  │              Next.js / PWA 모바일 반응형 웹                   │
  │  ├─ 지도 프리뷰, Foreground GPS, 외부 지도 연동                 │
  │  └─ [📱 로컬 스토리지 / IndexedDB]                           │
  │      • 반려견 프로필 (체급, 연령, 관절 상태)                    │
  │      • 개인 산책 이력 및 자택 출발지 좌표 (절대 서버 미저장)     │
  │      • 최근 보행 체감 피드백 (경사/계단 만족도)                 │
  └──────────────────────────────┬──────────────────────────────┘
                                 │ REST API (JSON)
                                 │ (Stateless: 로컬 컨텍스트를 요청 본문에 동봉)
                                 ▼
  ┌─────────────────────────────────────────────────────────────┐
  │                 FastAPI Backend & Validator                 │
  └──────────────────────────────┬──────────────────────────────┘
                                 │
                                 ▼
  ┌─────────────────────────────────────────────────────────────┐
  │                 Walk Planning AI Agent                      │
  │                      (LangGraph)                            │
  └──────────────────────────────┬──────────────────────────────┘
                                 │ 도구 오케스트레이션
        ┌───────────────────┬────┴──────────────┬───────────────────┐
        ▼                   ▼                   ▼                   ▼
 [ Routing Adapter ] [ Steps Detector ]  [ Slope Analyzer ]  [ Shade Estimator ]
  - ORS/OSRM 연동     - OSM Steps 조회    - DEM 고도 데이터    - SunCalc 태양각
  - Waypoints 생성    - 확인된 계단 배제  - 경사도 프로파일   - 건물 그림자 추정
        │                   │                   │                   │
        └───────────────────┴───────────────────┴───────────────────┘
                                 │
                                 ▼
                    [ Candidate Route Scoring ]
                    (다요소 가중 평가 후 최종 루프 선정)
                                 │
                    [ 커뮤니티 공유 / 제보 시에만 ]
                    (출발지 200m 마스킹 블러링 적용)
                                 ▼
  ┌─────────────────────────────────────────────────────────────┐
  │                 ☁️ Supabase Cloud (최소 관리)                 │
  │  • auth.users (간단한 이메일/비밀번호 계정)                   │
  │  • community_courses (공개 공유된 산책 코스)                │
  │  • hazard_reports (공사/높은 턱 현장 제보 사진 및 좌표)     │
  └─────────────────────────────────────────────────────────────┘
```

### 3.2 핵심 기술 요소 상세 명세
1. **AI Agent (Walk Planning Agent)**:
   - 클라이언트 로컬에서 전달받은 `DogProfile`과 사용자 발화를 파싱하여 `TargetDuration`, `AvoidStairs`, `SlopePreference`, `ShadePriority` 파라미터 추출.
   - Pydantic V2 기반 엄격한 스키마 검증을 거쳐 `search_routes`, `inspect_hazard`, `evaluate_candidates` 도구를 자율 오케스트레이션.
2. **Routing Adapter & Candidate Scorer**:
   - 전문 Routing API(OpenRouteService, OSRM 등)와 연동하여 실제 도로망 기반 복수 후보 경로를 수집.
   - OSM Steps 결측 가능성을 인지하고 지도에 명시된 `highway=steps` 링크를 우선 배제하며, 고도/그늘 지표를 종합 채점(Scoring)하여 최적 경로 결정.
3. **Multimodal Vision Inspector**:
   - Gemini 1.5 Flash 기반으로 현장 사진의 시각적 위험 요소를 진단:
     1. **현장 장애물 판독**: 높은 보도 턱, 확인되지 않은 야외 계단, 공사 잔해 감지.
     2. **안내판 판독**: 공원 입구 종합안내판의 반려견 출입 금지 구역 구조화.
4. **Local-First Data & Simple Email Auth**:
   - **사용자 폰(IndexedDB)**: 반려견 정보, 개인 산책 메모, 자택 출발지 좌표를 영구 보관하여 프라이버시 원천 보호.
   - **Supabase Cloud**: 소셜 OAuth 없이 **간단한 이메일/비밀번호 가입**만 지원하며, 커뮤니티 공개 코스 및 현장 위험 제보만 중앙 관리.
5. **Weather & Heat Risk Model**:
   - 기상청 단기예보(기온, 풍속, 운량)를 수집하여 시간대별 예상 체감 위험도를 산출하고 무더운 시간대를 피해 산책하도록 가이드.

---

## 4. 라우팅 비용 모델 및 조건 우선순위

```text
1. 통행 가능성 (보행자 통행 허용 구간, 사유지/통제구역 배제)
   ↓
2. 계단 회피 (OSM highway=steps 등 확인된 계단 우선 배제)
   ↓
3. 과도한 경사 회피 (max_slope_percent 제어, 완만한 경사 우선)
   ↓
4. 시간대별 그늘 우선 (태양 고도 및 건물 형상 기반 예상 그늘 가중치)
   ↓
5. 반려견별 선호 / 노면 선호 (부드러운 길 선호 반영, 단 안전조건보다 하위)
   ↓
6. 거리·시간·편의성 (목표 산책 시간 수렴 및 출발지 복귀)
```

$$\text{Cost}(e, t) = \text{Length}(e) \times F_{\text{steps}}(e) \times F_{\text{slope}}(e) \times F_{\text{shade}}(e, t) \times F_{\text{pref}}(e)$$

---

## 5. 데이터 신뢰도 및 프라이버시 체계

| 분류 | 데이터 항목 | 저장 위치 (Persistence) | 서비스 표현 및 보안 원칙 |
|---|---|---|---|
| **민감 개인정보** | 자택/출발지 위치, 상세 보행 GPS 궤적 | **사용자 휴대폰 (로컬 IndexedDB)** | **서버 절대 미전송**, 로컬 JSON 백업/복원 지원 |
| **반려견 신체정보** | 견종, 체중, 연령, 관절 주의 상태 | **사용자 휴대폰 (로컬 IndexedDB)** | 산책 생성 요청 시 Stateless Payload로만 전송 |
| **계정 정보** | 이메일, 비밀번호 해시 | **Supabase Cloud (`auth.users`)** | 소셜 로그인 배제, 간편 이메일/PW 로그인 |
| **커뮤니티 공개 데이터** | 공유 산책 코스, 후기 평점 | **Supabase Cloud (`community_courses`)** | **출발지 200m 마스킹 블러링 필수 적용** |
| **현장 위험 제보** | 높은 턱, 공사 구간, 야외 계단 사진 | **Supabase Cloud (`hazard_reports`)** | 사용자 식별 없이 공공 안전 제보로 공유 |

---

## 6. 서비스 주요 기능 명세

| 기능 분류 | 주요 기능 | 세부 설명 | 구현 기술 |
|---|---|---|---|
| **코어 플래닝** | **계단 회피·완만 경사 순환 코스 생성** | 목표 시간(15~60분)과 로컬 반려견 조건을 반영해 확인된 계단 및 급경사를 회피한 순환 산책로 추천 | LangGraph Agent + Routing API Adapter |
| **환경 최적화** | **시간대별 그늘 우선 코스 추천** | 출발 시각의 태양 위치와 건물 차폐 데이터를 연산하여 직사광선을 최소화하는 그늘길 안내 | Solar & Shadow Estimator |
| **현장 위험 분석** | **Vision 기반 턱/장애물 분석 및 우회** | 보행 중 맞닥뜨린 높은 턱, 공사 구간 사진 업로드 시 위험도 판독 및 안전 우회 안내 | Gemini 1.5 Flash Vision |
| **프라이버시 보관함**| **개인화 데이터 로컬 저장 & 백업** | 반려견 프로필, 개인 산책 이력을 폰에만 저장하고 JSON 파일로 안전하게 백업/복원 | IndexedDB + File System API |
| **커뮤니티 공유** | **안심 코스 공유 및 마스킹** | 완주한 멋진 코스를 이웃 견주에게 공유하되 출발지 200m는 자동 블러링 처리 | Supabase PostgreSQL + Spatial Jittering |
| **지도 및 길안내** | **경로 시각화 및 외부 지도 연동** | 구간별 경사/그늘 상태를 색상으로 지도에 표기하고 네이버/카카오 지도 길찾기 딥링크 제공 | MapLibre / Leaflet + 외부 딥링크 |
| **산책 기록** | **Foreground 위치 기록 및 요약** | 산책 세션 동안 화면 내 Foreground 위치를 수집하여 이동 거리, 소요 시간 요약 | Web Geolocation API |

---

## 7. 비기능 요구사항 및 모바일 UX 원칙

1. **프라이버시 바이 디자인 (Privacy by Design)**:
   - 사용자의 자택 주소나 일상 동선을 서버에 영구 수집하지 않는 Local-First 원칙을 준수.
2. **간결한 회원가입 (Simple & Independent Auth)**:
   - 외부 소셜 플랫폼(카카오/구글) 종속성을 배제하고, `Supabase Auth` 이메일/비밀번호 방식으로 1초 만에 가입 지원 (개발/CBT 중 Auto-confirm 활성화).
3. **모바일 웹/PWA 실현 가능성**:
   - 배터리 소모와 OS 제약이 큰 백그라운드 GPS 대신 **Foreground 위치 추적**을 기본 구조로 채택하고 상용 지도 앱 도보 길찾기 딥링크 연동.
4. **아키텍처 안정성**:
   - 예측 가능한 REST API 기반 요청/응답 구조 준수.
