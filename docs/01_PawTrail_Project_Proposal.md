# [프로젝트 기획서] 반려견 맞춤형 안심 산책 에이전트 및 기록 플랫폼 (PawTrail)

## 1. 프로젝트 개요
* **프로젝트명**: PawTrail (포트레일)
* **부제**: 반려견 관절 안심·무계단·완만 경사·그늘 우선 AI Native 산책 플래너 및 시선 해방(Eyes-Free) 핸즈프리 모바일 플랫폼
* **기획 배경 및 방향성**:
  - 도심지 반려견 산책에서 발생하는 실질적 위험은 **계단으로 인한 관절 부담, 가파른 경사로 인한 보행 피로, 한낮 직사광선/열 위험, 현장의 돌발 장애물(높은 턱, 공사)**입니다.
  - 기존 지도 서비스는 목적지 최단거리 위주의 보행 경로만을 제공하여 계단 유무나 경사, 그늘 상태를 알 수 없습니다.
  - **시선과 두 손의 자유 (Eyes-Free & Hands-Free)**: 하네스 줄을 잡고 스마트폰 화면을 위험하게 들여다보며 걷는 모바일 웹의 한계를 탈피하고, **스마트폰을 주머니에 넣고 귀로 안내를 듣는 React Native (Expo) 기반 백그라운드 핸즈프리 음성 길 안내**를 구현합니다.
  - **배포 피로도 제로 (EAS Build + EAS Update OTA)**: 복잡한 앱스토어 심사나 잦은 APK 재설치 없이, **EAS Build로 테스터 APK를 1회 배포**한 후 모든 기능 수정 및 핫픽스는 **EAS Update (`expo-updates`) 무중단 무선 OTA**로 실시간 반영합니다.
  - **프라이버시 중심 로컬 저장 (Local-First)**: 견주의 자택 위치, 상세 보행 GPS 궤적, 반려견 프로필 등 민감 정보는 **사용자의 폰 로컬 스토리지(`AsyncStorage`)에만 안전하게 보관**합니다.
  - **간단한 이메일 가입 & 커뮤니티 보호**: 외부 소셜 로그인의 번거로움과 종속성을 배제하고, **Supabase Auth 기반 간단한 이메일 가입**으로 커뮤니티 공유 시 **출발지 200m 마스킹 블러링**을 적용합니다.
* **핵심 타깃**:
  - 관절 안심 케어가 필요하여 계단이나 가파른 언덕을 피해야 하는 소형견·노령견 견주
  - 스마트폰 화면을 보지 않고 두 손으로 안전하게 리드줄을 통제하며 음성 안내로 산책하고 싶은 견주
  - 자택 위치 등 개인정보 유출 걱정 없이 안심 코스를 추천받고 기록하고 싶은 견주
* **실사용자 테스트 목표**: 반려견 견주 최소 5인 이상 대상 실제 필드 테스트 및 피드백 반영

---

## 2. 문제 정의 및 AI 기반 해결 방식

| 문제점 (Pain Points) | AI 및 데이터 기반 해결 방식 | 적용 기술 (필수 요건 충족) |
|---|---|---|
| **계단 통행으로 인한 관절 부담**<br>(소형견·노령견 관절 취약) | OSM 보행망의 `highway=steps` 속성을 사전에 필터링하여 **지도 데이터상 확인된 계단 구간을 최우선 배제(Hard Constraint)**하는 순환 경로 생성 (대체 경로 부재 시 사유 명시) | **OSM Steps Detector & Routing Adapter** |
| **과도한 경사로 인한 보행 피로**<br>(가파른 오르막·내리막 부상 위험) | DEM 고도 데이터 및 라우팅 고도 프로파일을 연계하여 **최대 경사도(`max_slope_percent`)를 제어**, 완만한 평지 위주 경로 도출 | **Elevation/DEM Slope Analyzer** |
| **직사광선 노출 및 열 스트레스**<br>(여름철 복사열 및 탈진 위험) | 산책 일시와 위치 기반 태양 고도/방위각(SunCalc)과 확보 가능한 건물 형상 데이터를 결합하여 **시간대별 예상 그늘 구간을 우선 배정** | **Solar Model & Shade Estimator (Level 1~2)** |
| **산책 중 폰 화면 주시의 위험성**<br>(리드줄 파지 중 화면 응시 시 낙상/사고) | 화면을 보며 걷는 위험을 원천 차단하기 위해, **주머니 속에서도 OSRM 스텝과 경로 속성을 결합해 회전 및 위험을 알려주는 백그라운드 핸즈프리 음성 안내** 제공 | **React Native Expo (Background GPS & Speech TTS)** |
| **현장 돌발 장애물 및 시각적 위험**<br>(높은 턱, 공사 구간, 깨진 유리) | 산책 전/중 현장 사진을 **Vision AI가 분석하여 높은 턱·계단·보행 방해 요소를 감지**하고 안전 우회 경로 권장 | **Multimodal Vision Inspector** |
| **자택 위치 및 개인정보 노출 우려**<br>(프라이버시 침해 우려) | 자택 위치, 보행 궤적, 반려견 프로필을 서버 DB에 모으지 않고 **폰 로컬 스토리지에 격리 보관**. AI 요청 시 일회성 페이로드로 전송하는 무상태(Stateless) 처리 | **Local-First Architecture (AsyncStorage)** |

---

## 3. 시스템 아키텍처 및 핵심 기술 명세

### 3.1 시스템 개념 아키텍처 다이어그램
```text
  [ 사용자 입력 (자연어 발화 / 산책 조건 선택 / 현장 사진) ]
                     │
                     ▼
  ┌─────────────────────────────────────────────────────────────┐
  │         React Native (Expo SDK 51+) 모바일 앱 인터페이스        │
  │  ├─ 시선 해방(Eyes-Free) 백그라운드 핸즈프리 음성 안내        │
  │  ├─ react-native-maps 구간별 색상 분기 코스 프리뷰          │
  │  └─ [📱 로컬 저장소] AsyncStorage (반려견 프로필, 개인 궤적)│
  └──────────────────────────────┬──────────────────────────────┘
                                 │ (HTTPS Stateless REST API)
                                 ▼
  ┌─────────────────────────────────────────────────────────────┐
  │         FastAPI Backend & LangGraph Walk Planning Agent     │
  └──────┬───────────────────────┬───────────────────────┬──────┘
         ▼                       ▼                       ▼
  [ Routing Adapter ]    [ Steps & Slope ]       [ Vision Inspector ]
   - ORS/OSRM 연동        - OSM 계단 배제         - Gemini 가용 체인
   - 루프 후보군 생성     - DEM 완만 경사도       - 현장 위험/안내판 판독
         │                       │                       │
         └───────────────────────┴───────────────────────┘
                                 │
                                 ▼
                    [ Candidate Route Scoring ]
                    (다요소 가중 평가 후 최종 루프 선정)
                                 │
                                 ▼ (커뮤니티 공유 시 200m 마스킹)
  ┌─────────────────────────────────────────────────────────────┐
  │                 ☁️ Supabase Cloud (최소 관리)                 │
  │  • auth.users (간단한 이메일/비밀번호 계정)                   │
  │  • community_courses (출발지 200m 마스킹 공개 산책 코스)     │
  │  • hazard_reports (공사/높은 턱 현장 제보 사진 및 좌표)     │
  └─────────────────────────────────────────────────────────────┘
```

### 3.2 핵심 기술 요소 상세 명세
1. **React Native (Expo) 클라이언트 & 핸즈프리 음성 안내 엔진**:
   - `expo-location`과 Android Foreground Service 기반 백그라운드 GPS 위치 추적.
   - `expo-speech` TTS와 OSRM 안내 스텝을 결합하여, 폰을 주머니에 넣은 채 *"50m 앞 완만한 길입니다. 우회전하세요"*, *"높은 턱 주의 구간입니다"* 등 실시간 음성 브리핑 송출.
   - **EAS Build로 1회 APK 배포** 후 모든 로직과 UI 변경은 **EAS Update (`expo-updates`) 무선 OTA**로 즉시 반영.
2. **AI Agent (Walk Planning Agent)**:
   - 클라이언트 로컬에서 전달받은 `DogProfile`과 사용자 발화를 파싱하여 `TargetDuration`, `AvoidStairs`, `SlopePreference`, `ShadePriority`, `PreferredSurfaces` 파라미터 추출.
   - Pydantic V2 기반 엄격한 스키마 검증을 거쳐 `search_routes`, `inspect_hazard`, `evaluate_candidates`, `search_parking` 도구를 자율 오케스트레이션.
3. **Routing Adapter & Candidate Scorer**:
   - 전문 Routing API(OpenRouteService, OSRM 등)와 연동하여 실제 도로망 기반 복수 순환 후보 경로를 수집.
   - OSM `highway=steps` 링크를 우선 배제하고 DEM 경사도 및 그늘 지표를 종합 채점하여 최적 경로 결정.
4. **Multimodal Vision Inspector (현장 위험물 & 공원 안내판)**:
   - Gemini 가용 모델 우선순위 선택 파이프라인(Gemini 3.5 Flash-Lite ➔ Gemini 3.1 Flash-Lite ➔ Gemini 3.6 Flash) 기반으로 2가지 핵심 시각 분석 수행:
     1. **공원 종합안내판 판독 (`ParkBoardInspector`)**: 공원 입구 오프라인 안내판 사진에서 흙길/잔디마당 산책로 범례와 반려견 출입 금지 구역을 파싱하여 구조화된 JSON(`ParkBoardInspectionResult`) 반환.
     2. **현장 위험물 진단 및 커뮤니티 제보 검증 (`Hazard & Surface Enricher`)**: 높은 턱, 야외 계단, 공사 자재 및 완주 후 견주의 현장 노면 사진을 시각 진단하여 안전 우회 경로 유도 및 지도 속성 보강.
5. **Local-First Data & Simple Email Auth**:
   - **사용자 폰(AsyncStorage)**: 반려견 정보, 개인 산책 메모, 자택 출발지 좌표, 나만의 코스 즐겨찾기를 영구 보관하여 프라이버시 원천 보호.
   - **Supabase Cloud**: 소셜 OAuth 없이 **간단한 이메일/비밀번호 가입**만 지원하며, 커뮤니티 공개 코스(출발지 200m 마스킹) 및 현장 위험 제보만 중앙 관리.
6. **자동화 워크플로우 (n8n Webhook Pipeline)**:
   - 매일 오전 기상청 단기예보를 수집하고 경험적 지면열 수지식을 실행:  
     $$\text{Estimated Surface Temp} = \text{Air Temp} + (\text{Insolation Weight} \times 15)$$
   - 아스팔트 온도가 35℃ 이하로 내려가는 안전 시간대(골든타임)를 산출해 푸시/웹훅 알림 발송.

---

## 4. 라우팅 비용 모델 및 조건 우선순위

```text
1. 통행 가능성 (보행자 통행 허용 구간, 사유지/통제구역 배제)
   ↓
2. 계단 회피 (OSM highway=steps 등 확인된 계단 우선 배제)
   ↓
3. 과도한 경사 회피 (max_slope_percent 제어, 완만한 경사 우선)
   ↓
4. 시간대별 그늘 우선 (태양 고도 및 건물 형상 기반 예상 그늘 가중치)
   ↓
5. 반려견별 선호 / 노면 선호 (부드러운 길 선호 반영, 단 안전조건보다 하위)
   ↓
6. 거리·시간·편의성 (목표 산책 시간 수렴 및 출발지 복귀)
```

$$\text{Cost}(e, t) = \text{Length}(e) \times F_{\text{steps}}(e) \times F_{\text{slope}}(e) \times F_{\text{shade}}(e, t) \times F_{\text{pref}}(e)$$

---

## 5. 데이터 신뢰도 및 프라이버시 체계

| 분류 | 데이터 항목 | 저장 위치 (Persistence) | 서비스 표현 및 보안 원칙 |
|---|---|---|---|
| **민감 개인정보** | 자택/출발지 위치, 상세 보행 GPS 궤적 | **사용자 휴대폰 (`AsyncStorage`)** | **서버 절대 미전송**, 로컬 JSON 백업/복원 지원 |
| **반려견 신체정보** | 견종, 체중, 연령, 관절 주의 상태 | **사용자 휴대폰 (`AsyncStorage`)** | 산책 생성 요청 시 Stateless Payload로만 전송 |
| **개인 보관함** | 나만의 안심 코스 즐겨찾기 북마크 | **사용자 휴대폰 (`AsyncStorage`)** | 오프라인에서도 즉시 재산책 연동 가능한 로컬 캐싱 |
| **계정 정보** | 이메일, 비밀번호 해시 | **Supabase Cloud (`auth.users`)** | 소셜 로그인 배제, 간편 이메일/PW 로그인 |
| **커뮤니티 공개 데이터** | 공유 산책 코스, 후기 평점 | **Supabase Cloud (`community_courses`)** | **출발지 200m 마스킹 블러링 필수 적용** |
| **현장 위험 제보** | 높은 턱, 공사 구간, 야외 계단 사진 | **Supabase Cloud (`hazard_reports`)** | 사용자 식별 없이 공공 안전 제보로 공유 |

---

## 6. 서비스 주요 기능 명세

| 기능 분류 | 주요 기능 | 세부 설명 | 구현 기술 |
|---|---|---|---|
| **코어 플래닝** | **계단 회피·완만 경사 순환 코스 생성** | 목표 시간(10~90분 슬라이더, 권장 15~60분)과 반려견 조건을 반영해 확인된 계단(OSM steps) 및 급경사를 회피한 순환 산책로 추천 | LangGraph Agent + Routing API Adapter |
| **핸즈프리 보행** | **시선 해방(Eyes-Free) 백그라운드 음성 안내** | 스마트폰을 주머니에 넣고 화면을 끈 채 OSRM 안내 스텝 및 노면 회전("50m 앞 완만한 길입니다. 우회전하세요")을 폰 스피커/이어폰으로 실시간 음성 송출 | React Native (Expo Background GPS & Speech TTS) |
| **환경 최적화** | **시간대별 그늘 우선 코스 추천** | 출발 시각의 태양 위치와 건물 차폐 데이터를 연산하여 직사광선을 최소화하는 그늘길 안내 | Solar & Shadow Estimator |
| **스케줄 자동화**| **기상 연동 지면열 안심 알림** | 기상청 기온/일사량 추정 수지식 기반 35℃ 이하 안전 산책 골든타임 알림 (n8n 연동) | n8n Automation + Open Weather API |
| **사전 비전 분석**| **공원 종합안내판 비전 판독 (`ParkBoardInspector`)** | 공원 입구 오프라인 안내판 사진 업로드 시 흙길/잔디마당 및 반려견 출입 금지 구역 사전 시각 판독 | Gemini Fallback Chain (3.5 Flash-Lite / 3.1 Flash-Lite / 3.6 Flash) |
| **현장 위험 분석**| **Vision 기반 턱/장애물 분석 및 커뮤니티 제보** | 보행 중 맞닥뜨린 높은 턱, 공사 구간 사진 업로드 시 위험도 판독 및 안전 우회 안내 | Gemini Fallback Chain + Supabase |
| **안심 거점 연계**| **주차장 P&R(Park & Walk) 코스 추천** | 차량 이동 견주를 위해 인근 공영주차장 정보를 실시간 연동해 주차 후 바로 산책하는 코스 추천 | Public Data API Tool |
| **프라이버시 보관함**| **개인화 데이터 로컬 저장 & 나만의 코스 즐겨찾기** | 반려견 프로필, 개인 산책 이력, 즐겨찾기 코스를 폰 로컬에만 저장하고 JSON 파일로 안전하게 백업/복원 | AsyncStorage + FileSystem |
| **커뮤니티 공유** | **안심 코스 공유 및 출발지 마스킹** | 완주한 멋진 코스를 이웃 견주에게 공유하되 출발지 200m는 자동 블러링 처리 | Supabase PostgreSQL + Spatial Jittering |
| **무중단 배포** | **EAS Build & EAS Update OTA** | 1회 APK 배포 후 모든 기능 수정 및 핫픽스를 무선 무점검 OTA로 실시간 업데이트 | Expo EAS CLI & GitHub Actions |

---

## 7. 비기능 요구사항 및 모바일 UX 원칙

1. **시선 해방 핸즈프리 안전성 (NFR-01)**:
   - 스마트폰 화면을 계속 응시하며 걷는 위험한 보행을 원천 방지하기 위해, **OSRM Steps 기반 핸즈프리 음성 안내**를 백그라운드로 제공하여 견주가 두 손으로 리드줄 통제에 전념할 수 있도록 보장.
2. **배포 및 무중단 업데이트 (NFR-02)**:
   - React Native (Expo) 기반 **EAS Build로 테스터용 APK 1회 배포**를 수행하며, 이후 모든 로직/UI 수정은 **EAS Update (`expo-updates`) 무중단 OTA 배포**를 통해 재설치 없이 실시간 반영.
3. **프라이버시 바이 디자인 (NFR-03)**:
   - 사용자의 자택 주소나 일상 동선을 서버에 영구 수집하지 않는 Local-First 원칙 준수. 커뮤니티 코스 공유 시 출발지/도착지 200m 반경 좌표 자동 마스킹.
4. **간결한 회원가입 (NFR-04)**:
   - 외부 소셜 플랫폼(카카오/구글) 종속성을 배제하고, `Supabase Auth` 이메일/비밀번호 방식으로 1초 만에 가입 지원 (개발/CBT 중 Auto-confirm 활성화).
5. **AI 윤리 및 의료 진단 배제 (NFR-05)**:
   - AI 분석 및 추천은 안전하고 편안한 산책 계획을 위한 참고 정보이며 의료적 진단이 아님을 고지.
6. **긍정적 웰니스 카피라이팅 가드레일 (NFR-06)**:
   - 앱 화면 UI, 온보딩, TTS 음성 스크립트, AI 프롬프트 전역에서 '슬개골 탈구' 등 의학적 질병 용어를 배제하고 "폭신한 길", "관절 안심 케어", "부드러운 잔디/흙길" 등 긍정적 웰니스 표현으로 일원화.

---

## 8. 실사용자(최소 5인) CBT 테스트 및 피드백 반영 계획 (US-H1)

### 8.1 5대 페르소나별 필드 테스트(CBT) 시나리오
| 테스터 | 대상 반려견 | 핵심 검증 목표 | 상세 테스트 시나리오 |
|---|---|---|---|
| **Tester 1** | 소형견 (말티즈 4세, 관절 안심 케어) | 흙/잔디길 고가중치($W_{\text{pref}}=0.4$), 계단 완전 배제, 시선 해방 핸즈프리 음성 안내 | "계단 없는 완만한 길 20분" 요청 ➔ 계단 0건 확인 및 주머니 속 스마트폰 음성 안내만으로 100% 핸즈프리 완주 |
| **Tester 2** | 대형견 (골든리트리버 2세, 활동견) | 주차장(P&R) 거점 출발 순환 코스 연계 및 그늘 우선 코스 | 차량 이동 후 공영주차장 P&R 거점 출발 40분 코스 생성 ➔ 주차 후 바로 산책 연계 검증 |
| **Tester 3** | 노령견 (포메라니안 9세, 노령견) | 탄성포장/완만 경사(최대 8% 이하) 및 15분 짧은 순환 코스 | 슬라이더 15분 설정 ➔ 급경사 배제 및 부드러운 우레탄/완만 평지 코스 주행 무결성 확인 |
| **Tester 4** | 일반 보행견 (믹스견 3세, 일반) | 현장 위험물(높은 턱/공사) 사진 제보 및 3초 이내 우회 리라우팅 | 산책 중 맞닥뜨린 높은 턱 촬영 ➔ Gemini Vision AI 판독 및 3초 내 우회 루프 코스 재탐색 |
| **Tester 5** | 주간 보행견 (시츄 6세, 관절 안심) | n8n 지면열 안전 골든타임 알림 수신 후 완주 및 3초 원터치 피드백 | 35℃ 이하 지면열 안전 골든타임 알림 확인 ➔ 산책 후 "경사도 적절함" 3초 피드백 제출 및 로컬 적재 |

### 8.2 테스트 절차 및 E2E 라이프사이클
1. **APK 설치 및 온보딩**: EAS Build로 배포된 APK 설치 후 반려견 프로필(견종, 체급, 관절 안심 케어 선호) 기기 로컬(`AsyncStorage`) 등록
2. **조건별 순환 코스 요청**: 사용자 산책 조건 설정 (예: 10~90분 슬라이더, 선호 노면 선택)
3. **핸즈프리 보행 수행**: 스마트폰을 주머니에 넣고 백그라운드 GPS 및 `expo-speech` 음성 브리핑에 따라 안전하게 보행
4. **현장 위험 분석 검증**: 산책 중 발견한 높은 턱/공사 구간 사진을 촬영·업로드하여 Gemini Flash 체인 위험 판독 및 우회 검증
5. **완주 기록 및 피드백**: 산책 종료 후 나만의 코스 즐겨찾기 저장 및 3초 피드백 제출 (무상태 다음 플래닝 연동)

### 8.3 피드백 반영 및 무선 OTA 배포 체계
* 수집된 5인 피드백을 바탕으로 경사도 가중치($W_{\text{slope}}$), 그늘 지표 가중치($W_{\text{shade}}$) 및 에이전트 프롬프트 즉각 미세 튜닝
* 수정 사항은 `eas update` 무선 OTA로 즉시 배포하여 테스터가 재설치 없이 다음 산책 시 개선된 기능을 즉시 체감할 수 있도록 구현

---

## 9. 향후 비즈니스 확장 모델

* **반려견 동반 상권 연계**: 안심 산책로 주변 펫 프렌들리 카페, 동물병원, 펫숍 할인 쿠폰 매핑 (주차장 P&R 및 경유지 연계).
* **지자체 반려견 인프라 개선 데이터 제공**: 견주들의 현장 위험 제보 및 통행 데이터를 익명 분석해 '보행로 턱 낮춤 공사', '반려견 배변 봉투함 설치' 최적 입지 리포트 공급.
