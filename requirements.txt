# ==============================================================================
# PawTrail (포트레일) - Python Backend & AI Engine Dependencies
# ==============================================================================

# --- [Web Framework & Server Engine] ---
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
python-multipart>=0.0.9                  # 안내판/현장 사진 업로드 (UploadFile, File)

# --- [Data Validation & Settings] ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0

# --- [AI Agent & LLM & Vision AI] ---
# Gemini 1.5 Flash Vision (공원 안내판 판독, 단차/위험물 분석) & LangGraph Agent
google-genai>=2.0.0
google-generativeai>=0.8.0
langgraph>=0.1.0
langchain>=0.2.0
langchain-core>=0.2.0

# --- [GIS, Spatial Processing & Routing] ---
# OSM 보행망 계단 회피(highway=steps), DEM 경사도 및 SunCalc 그늘길 분석, OSRM/ORS 라우팅
shapely>=2.0.0
pyproj>=3.6.0                            # 좌표계 변환 (WGS84 EPSG:4326 등)
suncalc>=0.1.2                           # 태양 위치 및 실시간 그늘길 계산
networkx>=3.2                            # 보행 네트워크 그래프 분석 및 루프 탐색
openrouteservice>=2.3.0                  # OpenRouteService Routing API 어댑터
geojson>=3.1.0

# --- [Image Processing (Vision Input)] ---
pillow>=10.3.0

# --- [HTTP & Async Clients] ---
httpx>=0.27.0                            # 비동기 HTTP 클라이언트 (기상청 API, OSRM)
requests>=2.31.0

# --- [Database & Cloud Backend] ---
# 200m 마스킹 커뮤니티 코스 및 현장 위험 제보 관리
supabase>=2.4.0

# --- [Environment & Utilities] ---
python-dotenv>=1.0.1

# --- [Testing & Quality Assurance] ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
